diff --git a/KrkrPatch/dllmain.cpp b/KrkrPatch/dllmain.cpp
index ecb9c79..a5a2a26 100644
--- a/KrkrPatch/dllmain.cpp
+++ b/KrkrPatch/dllmain.cpp
@@ -21,0 +22,2 @@ static HMODULE g_hDLL;
+static INIT_ONCE g_initializeOnce = INIT_ONCE_STATIC_INIT;
+static BOOL g_startupResult = FALSE;
@@ -30 +32 @@ template<class T>
-void InlineHook(T& OriginalFunction, T DetourFunction)
+bool InlineHook(T& OriginalFunction, T DetourFunction)
@@ -32,4 +34,9 @@ void InlineHook(T& OriginalFunction, T DetourFunction)
-	DetourUpdateThread(GetCurrentThread());
-	DetourTransactionBegin();
-	DetourAttach(&(PVOID&)OriginalFunction, (PVOID&)DetourFunction);
-	DetourTransactionCommit();
+	if (DetourTransactionBegin() != NO_ERROR)
+		return false;
+	if (DetourUpdateThread(GetCurrentThread()) != NO_ERROR ||
+		DetourAttach(&(PVOID&)OriginalFunction, (PVOID&)DetourFunction) != NO_ERROR)
+	{
+		DetourTransactionAbort();
+		return false;
+	}
+	return DetourTransactionCommit() == NO_ERROR;
@@ -40 +47 @@ template<class T>
-void UnInlineHook(T& OriginalFunction, T DetourFunction)
+bool UnInlineHook(T& OriginalFunction, T DetourFunction)
@@ -42,4 +49,9 @@ void UnInlineHook(T& OriginalFunction, T DetourFunction)
-	DetourUpdateThread(GetCurrentThread());
-	DetourTransactionBegin();
-	DetourDetach(&(PVOID&)OriginalFunction, (PVOID&)DetourFunction);
-	DetourTransactionCommit();
+	if (DetourTransactionBegin() != NO_ERROR)
+		return false;
+	if (DetourUpdateThread(GetCurrentThread()) != NO_ERROR ||
+		DetourDetach(&(PVOID&)OriginalFunction, (PVOID&)DetourFunction) != NO_ERROR)
+	{
+		DetourTransactionAbort();
+		return false;
+	}
+	return DetourTransactionCommit() == NO_ERROR;
@@ -93 +105,3 @@ tTJSBinaryStream* _fastcall HookTVPCreateStream(const ttstr& name, tjs_uint flag
-				auto patchname = TVPGetAppPath() + arc.c_str();
+				try
+				{
+					auto patchname = TVPGetAppPath() + arc.c_str();
@@ -95,4 +109,4 @@ tTJSBinaryStream* _fastcall HookTVPCreateStream(const ttstr& name, tjs_uint flag
-				if (StringHelper::EndsWith(arc, L".xp3"))
-					patchname += L">";
-				else
-					patchname += L"/";
+					if (StringHelper::EndsWith(arc, L".xp3"))
+						patchname += L">";
+					else
+						patchname += L"/";
@@ -100 +114 @@ tTJSBinaryStream* _fastcall HookTVPCreateStream(const ttstr& name, tjs_uint flag
-				patchname += inarcname;
+					patchname += inarcname;
@@ -102 +116 @@ tTJSBinaryStream* _fastcall HookTVPCreateStream(const ttstr& name, tjs_uint flag
-				spdlog::debug("Find {}", Encoding::Utf16ToUtf8(patchname.c_str()));
+					spdlog::debug("Find {}", Encoding::Utf16ToUtf8(patchname.c_str()));
@@ -104,3 +118,3 @@ tTJSBinaryStream* _fastcall HookTVPCreateStream(const ttstr& name, tjs_uint flag
-				if (TVPIsExistentStorageNoSearchNoNormalize(patchname))
-				{
-					spdlog::debug("Open {}", Encoding::Utf16ToUtf8(patchname.c_str()));
+					if (TVPIsExistentStorageNoSearchNoNormalize(patchname))
+					{
+						spdlog::debug("Open {}", Encoding::Utf16ToUtf8(patchname.c_str()));
@@ -108 +122,12 @@ tTJSBinaryStream* _fastcall HookTVPCreateStream(const ttstr& name, tjs_uint flag
-					return pfnTVPCreateStream(patchname, flags);
+						auto stream = pfnTVPCreateStream(patchname, flags);
+						if (stream)
+							return stream;
+					}
+				}
+				catch (const std::exception& e)
+				{
+					spdlog::error("Couldn't to open a localization stream: {}", e.what());
+				}
+				catch (...)
+				{
+					spdlog::error("Couldn't to open a localization stream.");
@@ -130,3 +155,4 @@ void InstallStreamHook()
-		InlineHook(pfnTVPCreateStream, HookTVPCreateStream);
-
-		spdlog::debug("Hook installed.");
+		if (InlineHook(pfnTVPCreateStream, HookTVPCreateStream))
+			spdlog::debug("Hook installed.");
+		else
+			spdlog::error("Couldn't to install stream hook.");
@@ -153,5 +179,20 @@ HRESULT _stdcall HookV2Link(iTVPFunctionExporter* exporter)
-	pfnTVPV2Link(exporter);
-	UnInlineHook(pfnTVPV2Link, HookV2Link);
-	TVPInitImportStub(exporter);
-	InstallStreamHook();
-	return 1;
+	auto result = pfnTVPV2Link(exporter);
+	if (!UnInlineHook(pfnTVPV2Link, HookV2Link))
+	{
+		spdlog::error("Couldn't to remove V2Link hook.");
+		return result;
+	}
+	try
+	{
+		TVPInitImportStub(exporter);
+		InstallStreamHook();
+	}
+	catch (const std::exception& e)
+	{
+		spdlog::error("Couldn't to initialize stream hook: {}", e.what());
+	}
+	catch (...)
+	{
+		spdlog::error("Couldn't to initialize stream hook.");
+	}
+	return result;
@@ -170 +211 @@ FARPROC WINAPI HookGetProcAddress(HMODULE hModule, LPCSTR lpProcName)
-		if (strcmp(lpProcName, "V2Link") == 0)
+		if (result && strcmp(lpProcName, "V2Link") == 0)
@@ -174,2 +215,5 @@ FARPROC WINAPI HookGetProcAddress(HMODULE hModule, LPCSTR lpProcName)
-			UnInlineHook(pfnGetProcAddress, HookGetProcAddress);
-			InlineHook(pfnTVPV2Link, HookV2Link);
+			if (!UnInlineHook(pfnGetProcAddress, HookGetProcAddress) ||
+				!InlineHook(pfnTVPV2Link, HookV2Link))
+			{
+				spdlog::error("Couldn't to install V2Link hook.");
+			}
@@ -183 +227 @@ FARPROC WINAPI HookGetProcAddress(HMODULE hModule, LPCSTR lpProcName)
-void InstallHook()
+bool InstallHook()
@@ -185 +229 @@ void InstallHook()
-	InlineHook(pfnGetProcAddress, HookGetProcAddress);
+	return InlineHook(pfnGetProcAddress, HookGetProcAddress);
@@ -189 +233 @@ void InstallHook()
-void LoadConfig()
+bool LoadConfig()
@@ -200,2 +244 @@ void LoadConfig()
-			Util::ThrowError(L"Couldn't to open file: %s", cfgPath.c_str());
-			return;
+			return false;
@@ -208,0 +252,2 @@ void LoadConfig()
+		if (g_logLevel < 0 || g_logLevel > 4)
+			return false;
@@ -247,0 +293 @@ void LoadConfig()
+		return true;
@@ -251,2 +297,2 @@ void LoadConfig()
-		Util::ThrowError("Couldn't to load config file: %s", e.what());
-		return;
+		OutputDebugStringA(e.what());
+		return false;
@@ -257 +303 @@ void LoadConfig()
-void OnStartup()
+bool OnStartup()
@@ -259 +305,2 @@ void OnStartup()
-	LoadConfig();
+	if (!LoadConfig())
+		return false;
@@ -267 +314,6 @@ void OnStartup()
-	InstallHook();
+	if (!InstallHook())
+	{
+		spdlog::error("Couldn't to install GetProcAddress hook.");
+		return false;
+	}
+	return true;
@@ -280,0 +333,27 @@ void OnShutdown()
+BOOL CALLBACK InitializeOnce(PINIT_ONCE, PVOID, PVOID*)
+{
+	try
+	{
+		g_startupResult = OnStartup() ? TRUE : FALSE;
+	}
+	catch (const std::exception& e)
+	{
+		OutputDebugStringA(e.what());
+		g_startupResult = FALSE;
+	}
+	catch (...)
+	{
+		g_startupResult = FALSE;
+	}
+	return TRUE;
+}
+
+
+extern "C" __declspec(dllexport) BOOL InitializeKrkrPatch()
+{
+	if (!InitOnceExecuteOnce(&g_initializeOnce, InitializeOnce, nullptr, nullptr))
+		return FALSE;
+	return g_startupResult;
+}
+
+
@@ -282,0 +362,5 @@ BOOL APIENTRY DllMain(HMODULE hModule, DWORD  ul_reason_for_call, LPVOID lpReser
+	if (DetourIsHelperProcess())
+	{
+		return TRUE;
+	}
+
@@ -286,0 +371 @@ BOOL APIENTRY DllMain(HMODULE hModule, DWORD  ul_reason_for_call, LPVOID lpReser
+			DetourRestoreAfterWith();
@@ -289 +374,2 @@ BOOL APIENTRY DllMain(HMODULE hModule, DWORD  ul_reason_for_call, LPVOID lpReser
-			OnStartup();
+			if (GetEnvironmentVariableW(L"KRKRPATCH_DEFER_STARTUP", nullptr, 0) == 0)
+				InitializeKrkrPatch();
@@ -302 +388,2 @@ BOOL APIENTRY DllMain(HMODULE hModule, DWORD  ul_reason_for_call, LPVOID lpReser
-			OnShutdown();
+			if (g_startupResult)
+				OnShutdown();
